import React, { useContext } from "react";
import { toast } from "react-toastify";
import { AuthContext } from "../../../context/UserContext";

const ServiceContainer = () => {
  return (
    <div>
      <h1>this is service container</h1>
    </div>
  );
};

export default ServiceContainer;
